/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "ik.imagekit.io",
      'images.unsplash.com',
      "static.vecteezy.com"
    ],
  },
};

export default nextConfig;
